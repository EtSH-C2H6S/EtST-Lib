package com.c2h6s.etstlib.data.providers;

import com.c2h6s.etstlib.EtSTLib;
import com.c2h6s.etstlib.util.EtSTLibTags;
import net.minecraft.data.PackOutput;
import net.minecraftforge.common.data.ExistingFileHelper;
import slimeknights.tconstruct.common.TinkerTags;
import slimeknights.tconstruct.library.data.tinkering.AbstractMaterialTagProvider;
import slimeknights.tconstruct.tools.data.material.MaterialIds;

//Only For Testing.
//测试用的别看了。

/*
public class EtSTLibMaterialTagProvider extends AbstractMaterialTagProvider {
    public EtSTLibMaterialTagProvider(PackOutput packOutput, ExistingFileHelper existingFileHelper) {
        super(packOutput, EtSTLib.MODID, existingFileHelper);
    }

    @Override
    protected void addTags() {
        this.tag(EtSTLibTags.WHITELISTED_RANDOM_MATERIAL).add(MaterialIds.wood);
        this.tag(EtSTLibTags.BLACKLISTED_RANDOM_MATERIAL).add(TinkerTags.Materials.NETHER.location());
    }

    @Override
    public String getName() {
        return "EEEEEtST Lib Material Tag Provider";
    }
}
 */
